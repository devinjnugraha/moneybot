# Postgres — Neon serverless Postgres (pooled `-pooler` endpoint).
# Paste your Neon connection string verbatim; KEEP ?sslmode=require (Neon refuses non-SSL).
# This is the DB `npm test` truncates — NEVER put the prod URL here.
DATABASE_URL=postgresql://USER:PASSWORD@ep-PROJECT-pooler.REGION.aws.neon.tech/neondb?sslmode=require

# Telegram
TELEGRAM_BOT_TOKEN=000000000:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenRouter
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_MODEL=anthropic/claude-3-haiku

# Tunables
CONTEXT_WINDOW_TURNS=20
SESSION_IDLE_TIMEOUT_MINUTES=30
CRON_SCHEDULE=0 8 * * *

# Proactive outreach (slice 1 — daily LLM summary at 21:00 WIB)
PROACTIVE_ENABLED=true
PROACTIVE_SUMMARY_CRON=0 21 * * *
PROACTIVE_MAX_PER_DAY=5
PROACTIVE_QUIET_HOURS=22:00-07:00
