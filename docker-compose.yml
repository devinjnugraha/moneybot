services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: moneybot
      POSTGRES_PASSWORD: moneybot
      POSTGRES_DB: moneybot
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
